---
name: private-backlog
description: 팀엔 안 보이는 PM 전용 비공개 백로그. 아이디어·리스크·관찰·나중에 볼 것을 로컬에 쌓고 조회한다. 절대 공유·전송하지 않는다. "백로그에 적어둬 / 아이디어 메모 / 나중에 볼 것 / 내 백로그 보여줘" 요청 시.
---

# PM 전용 비공개 백로그 (🔒 로컬 전용)

머릿속에만 있는 아이디어·찜찜함·관찰을 흘리지 않고 쌓는 개인 공간. **팀에 공개되지 않는다.**

## ⛔ 프라이버시 규칙
- 저장 위치는 로컬 `data/backlog.md`(git 제외). **어떤 채널로도 전송·공유하지 않는다.**
- 슬랙/노션 전송 스크립트에 넘기지 않는다. 이건 순수 개인 메모다.

## 담기
사용자가 아이디어/리스크/관찰을 말하면 바로 기록한다:
```bash
python3 "$CLAUDE_PLUGIN_ROOT/scripts/journal.py" --log backlog --add "온보딩 이탈 3단계에서 큼 — 진행바 실험 아이디어 (리스크: 개발공수)"
```
잘 담으려면 한 줄에 **무엇 + (있으면) 왜/맥락**을 함께. 분류 태그(아이디어/리스크/관찰)를 앞에 붙여도 좋다.

## 꺼내기 · 정리
```bash
python3 "$CLAUDE_PLUGIN_ROOT/scripts/journal.py" --log backlog --list
python3 "$CLAUDE_PLUGIN_ROOT/scripts/journal.py" --log backlog --list --since 14
```
- 요청 시 주제별로 묶어 보여주고, **묵힌 항목**(오래된 것)을 리마인드한다.
- 사용자가 원하면 특정 항목을 `prioritize`(우선순위)나 `write-spec`(기획서)로 넘긴다.

## 브리핑 연계
`daily-brief`/`weekly-review`가 만들어질 때, 관련된 백로그 항목이 있으면 **개인본에만** "묵혀둔 것 중 오늘 볼 것"으로 살짝 끌어와 상기시킨다(팀 공유본엔 넣지 않는다).

## 원칙
없는 걸 지어내 채우지 않는다. 사용자의 말/관찰만 기록. 판단·실행은 사람이.
